#ifndef _AFX_HPP_
#define _AFX_HPP_

#include <opencv2/opencv.hpp>
#include <algorithm>
#include <cmath>
#include <functional>
#include <vector>
#include <list>
#include <cassert>
#include <limits>

#ifndef MY_MACRO
#define HREP(Height) for (int iHeight = 0; iHeight < (Height); ++ iHeight)
#define WREP(Width) for (int iWidth = 0; iWidth < (Width); ++ iWidth)
#endif// ! MY_MACRO

#endif// ! _AFX_HPP_
